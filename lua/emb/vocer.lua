engine.name = 'Vocer'

local gate = {0, 0, 0, 0}
local freeze = {0, 0, 0, 0}

init = function()
end

gridkey = function(x, y, z)
   if(y == 8) then
      
   end
end

function toggle_freeze(i) end
function toggle_gate(i) end
